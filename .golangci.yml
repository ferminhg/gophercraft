# https://golangci-lint.run/docs/configuration/file
version: "2"

run:
  timeout: 5m

linters:
  default: none
  enable:
    - errcheck
    - govet
    - revive
    - staticcheck
    - unused

formatters:
  enable:
    - gofmt
    - goimports
