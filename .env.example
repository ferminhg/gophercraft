# Copy to `.env` for local overrides (see README).
HTTP_ADDR=:3000
LOG_LEVEL=info

# Human-readable logs in the terminal (JSON is better for production / log pipelines).
# LOG_PRETTY=1

# OpenTelemetry-style fields on every log line (optional).
# OTEL_SERVICE_NAME=gophercraft
# DEPLOYMENT_ENVIRONMENT=development
